import { StyleSheet} from "react-native";
export const Splash_style = StyleSheet.create({
    container: {
        marginTop:200,
        marginLeft:29,
    },
  });
  
  // export default reactnavitepage_style;
  